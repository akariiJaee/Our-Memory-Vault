rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // Only the two accounts you create in Authentication > Users can read or write.
    match /{allPaths=**} {
      allow read, write: if request.auth != null;
    }
  }
}
